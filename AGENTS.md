# Git 提交与推送规则

1. 未经用户明确确认，禁止直接执行 `git commit` 和 `git push`。
2. 根据实际修改内容生成中文 Commit 标题和中文 Commit 内容，交给用户审核。
3. 用户明确确认后才能执行 `git commit`。

# 项目运行规则

1. 本地主要用于代码修改、检查和 Git 管理，正式数据处理、训练和评估均在服务器执行。
2. 项目数据集存放在服务器，不要在本地下载、复制或构造完整数据集。
3. 涉及真实数据、GPU、大规模计算或完整实验时，应在服务器环境中执行。
4. 服务器为租赁服务器，平时可能处于关闭状态；如果无法连接服务器，不要反复尝试连接或长时间排查，应先判断服务器可能未启动，并停止无意义的重试。
5. 未经用户确认，不要删除、覆盖服务器上的数据、模型、日志或实验结果。